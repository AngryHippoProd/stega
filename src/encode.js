import React, { useState, useCallback } from 'react';
//grab a photo from imageViewer
import {ImageViewing} from './imageViewer';

export const encode = (image) =>{
  console.log(image);
}//index pixels
//change pixels
//instantiate new image with changed pixels
//display image
